# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — AERP Python Dependencies
#
# Grouped by purpose so you know WHY each package is here.
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5            # Web framework — async, auto-docs, Pydantic-native
uvicorn[standard]==0.32.1  # ASGI server that runs FastAPI
python-multipart==0.0.12   # Required for FastAPI file/form uploads

# ── LangChain — LLM building blocks ──────────────────────────────────────────
langchain==0.3.13                        # Core LangChain framework
langchain-core==0.3.28                   # Base abstractions (Runnables, Messages)
langchain-community==0.3.13             # Community integrations (document loaders)
langchain-google-genai==2.0.7           # Gemini LLM + embeddings

# ── LangGraph — Agent orchestration ──────────────────────────────────────────
langgraph==0.3.0                        # Graph-based workflow orchestration
langgraph-checkpoint-redis==0.0.4       # Redis checkpointer for HITL pause/resume

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36             # ORM for database models
asyncpg==0.30.0                          # Async PostgreSQL driver
psycopg2-binary==2.9.10                 # Sync driver (needed by Alembic migrations)
alembic==1.14.0                          # Database migrations (schema versioning)
pgvector==0.2.5                          # pgvector Python client (vector type support)
langchain-postgres==0.0.12              # LangChain's pgvector integration

# ── Cache + Task Queue ────────────────────────────────────────────────────────
redis==5.2.1                             # Redis client
celery[redis]==5.4.0                    # Distributed task queue
flower==2.0.1                           # Celery web UI (monitor tasks)

# ── GitHub Integration ────────────────────────────────────────────────────────
PyGithub==2.5.0                         # GitHub REST API client

# ── Jira Integration ──────────────────────────────────────────────────────────
jira==3.8.0                             # Jira REST API client

# ── Google Integration ────────────────────────────────────────────────────────
google-api-python-client==2.154.0      # Google APIs (Docs, Drive)
google-auth-httplib2==0.2.0            # HTTP transport for Google auth
google-auth-oauthlib==1.2.1            # OAuth flow for Google APIs

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic-settings==2.6.1               # Settings from .env file (type-validated)
python-dotenv==1.0.1                   # Load .env files

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx==0.28.1                           # Async HTTP client (for API calls)
tenacity==9.0.0                         # Retry logic with exponential backoff
structlog==24.4.0                       # Structured logging (JSON logs)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-mock==3.14.0
langchain-openai
langchain-anthropic==0.3.0
langchain-ollama==0.2.0
